/var/log/ceilometer/ceilometer-agent-notification.log {
    daily
    missingok
    compress
    delaycompress
    notifempty
    postrotate
        if dpkg-vendor --derives-from ubuntu ; then
            service ceilometer-agent-notification restart >/dev/null 2>&1 || true
        else
            invoke-rc.d ceilometer-agent-notification restart >/dev/null 2>&1 || true
        fi
    endscript
}
