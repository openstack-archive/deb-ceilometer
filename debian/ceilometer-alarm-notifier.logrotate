/var/log/ceilometer/ceilometer-alarm-notifier.log {
    daily
    missingok
    compress
    delaycompress
    notifempty
    postrotate
        if dpkg-vendor --derives-from ubuntu ; then \
            service ceilometer-alarm-notifier restart >/dev/null 2>&1 || true ; \
        else \
            invoke-rc.d ceilometer-alarm-notifier restart >/dev/null 2>&1 || true ; \
        fi
    endscript
}
